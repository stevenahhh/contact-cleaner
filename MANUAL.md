# 주소록 정리 v1.3.6 상세 매뉴얼

본 프로그램은 다량의 연락처 데이터를 정리, 비교, 병합하기 위해 설계된 도구입니다.

---

## 1. 주요 기능 상세 안내

### [Step 1. 정리] - 데이터 표준화
원본 데이터(CSV, Excel)에서 유효한 휴대폰 번호를 찾아내어 표준 형식(`010-XXXX-XXXX`)으로 변환합니다.

*   **자동 헤더 인식**: 파일 내의 '이름', '전화번호' 컬럼을 자동으로 탐색합니다. 헤더 명칭이 다르더라도 데이터 패턴을 분석하여 인식합니다.
*   **번호 정규화**: `01012345678`, `+82 10 1234 5678`, `010.1234.5678` 등 다양한 입력을 모두 표준 형식으로 변환합니다.
*   **중복 제거**: 동일한 번호가 여러 개 존재할 경우 하나만 남깁니다.

#### 파일명 형식 (중요)
**`{소유자명}_주소록` 포맷은 반드시 맞춰야 합니다.**

✅ **올바른 예시:**
- `홍길동_주소록.csv`
- `홍길동_주소록(구글).csv`
- `홍길동_주소록(1).xlsx`
- `홍길동_주소록_백업.csv`

❌ **잘못된 예시:**
- `홍길동주소록.csv` (언더스코어 없음)
- `주소록_홍길동.csv` (순서 바뀜)
- `홍길동.csv` ("_주소록" 없음)

**규칙:**
- `{소유자명}_주소록` 형식을 반드시 포함해야 합니다
- `_주소록` 뒤에 추가 글자를 작성해도 됩니다 (예: `_주소록(구글)`)
- 확장자는 `.csv` 또는 `.xlsx` 사용
- 결과 파일명에 소유자명이 자동으로 포함됩니다 (예: `1_정리_홍길동_260123-1234.xlsx`)

### [Step 2. 대조] - 데이터 비교
기존 주소록(기준)과 새로운 파일(대상)을 비교하여 일치 여부를 판별합니다.

*   **상태 마커**:
    *   `O`: 이름과 번호가 모두 일치 (안전한 중복)
    *   `△(번호)`: 번호는 같지만 이름이 다름 (확인 필요)
    *   `△(이름)`: 이름은 같지만 번호가 다름 (확인 필요)
    *   `X`: 기준 주소록에 존재하지 않는 새로운 데이터
*   **X 항목 제외 옵션**: 새로운 연락처만 골라내고 싶을 때, 기존에 없는 데이터(X)를 결과에서 제외하고 저장할 수 있습니다.

#### 파일명 형식 (중요)
**Step 1과 동일하게 `{소유자명}_주소록` 포맷을 맞춰야 합니다.**

결과 파일명에 소유자명이 자동으로 포함됩니다 (예: `대조_홍길동_260123-1234.xlsx`)

### [Step 3. 병합] - 최종 데이터 생성
여러 명의 주소록 결과물 중 필요한 것만 골라 하나의 최종 마스터 파일을 만듭니다.

*   **선택적 병합**: 각 파일의 '선택병합' 컬럼에 'O' 표시를 한 항목만 병합 대상이 됩니다.
*   **소유자 관리**: 병합 후 어느 파일에서 온 데이터인지 추적할 수 있도록 '주소록 소유자명'을 자동으로 기록합니다.

#### 파일명 형식 (중요)
병합 단계에서는 **정리/대조 결과 파일**을 사용합니다.

✅ **올바른 예시:**
- `1_정리_홍길동_260123-1234.xlsx` (Step 1 결과물)
- `대조_홍길동_260123-1234.xlsx` (Step 2 결과물)

이 파일들은 Step 1 또는 Step 2를 완료하면 자동으로 생성되는 파일명입니다.

---

## 2. 권장 워크플로우

1.  **표준화**: 모든 원본 파일을 [Step 1]에서 정리합니다.
    - 원본 파일명: `홍길동_주소록(구글).csv` 형식으로 준비
    - 결과: `1_정리_홍길동_260123-1234.xlsx` 생성
2.  **비교**: [Step 2]를 통해 기존 주소록과 대조하여 중복을 확인합니다.
    - 원본 파일명: `홍길동_주소록(1).csv` 형식으로 준비
    - 결과: `대조_홍길동_260123-1234.xlsx` 생성
3.  **검토**: 생성된 엑셀 파일을 열어 '선택병합' 칸에 병합할 항목을 표시합니다.
4.  **병합**: [Step 3]에서 정리/대조 결과 파일들을 넣어 최종 파일을 생성합니다.
    - 입력: `1_정리_홍길동_...xlsx`, `대조_김철수_...xlsx` 등
    - 결과: `병합결과_260123-1234.xlsx` 생성

---

## 3. 기술적 참고 사항

*   **지원 형식**: `.csv` (UTF-8/CP949 자동 감지), `.xlsx`, `.xls`
*   **저장 위치**: 프로그램 위치에 `작업결과` 폴더를 생성하고 날짜+시간별로 저장됩니다.
*   **파일 열기**: 로그창의 파일 경로를 클릭하면 해당 결과 파일이 즉시 열립니다.

---
**주소록 정리 v1.3.6**
*Created by custom orchestrator by 순광 안*
